    module.exports = {
      presets: [
        ['@babel/preset-env', { targets: { node: 'current' } }],
        // Add @babel/preset-react if using React
        // Add @babel/preset-typescript if using TypeScript
      ]
      // Add any necessary plugins here
      // plugins: [...]
    };